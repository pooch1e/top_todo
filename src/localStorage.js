//localStorage.js
//logic for storing cards

class Storage {
  constructor(card) {
    libraryArr = [];
    this.card = card;
  }
  pushCardToArray() { //will need to capture some kind of form
    const card = document.querySelector("submit-btn");
    
  }
}